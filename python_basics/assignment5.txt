Assignment- 5
Shreya Bag

1.> dictionary_name={ }

2.> {'foo': 42}

3.> A list refers to a collection of various index value pairs like that in the case of an array.
A dictionary refers to a hashed structure of various pairs of keys and values.

4.> KeyError error, cause 'foo' keyword is not included in the 'spam' dictionary.

5.> There is no difference. The in operator checks whether a value exists as a key in the dictionary.

6.> 'cat' in spam checks whether the value exists as a key in the dictionary.
'cat' in spam.values() checks whether there is value 'cat' for a key in the dictionary.

7.> spam.setdefault('color', 'black')

8.> pprint.pprint()